<footer class='bg-darker pt-16 pb-8'>
  <div class='container-custom'>
    <div class='grid md:grid-cols-5 gap-8 mb-12'>
      <div class='md:col-span-2'>
        <a href='/' class='group mb-4 inline-block'>
          <span class='text-3xl font-bold text-white group-hover:text-primary transition-colors duration-300'>
            {{ shop.name }}
          </span>
        </a>
        <p class='text-light mb-6 max-w-md'>
          Unlock Your Mental Edge with our scientifically formulated nootropic drink mix designed for peak cognitive
          performance.
        </p>

        <div class='flex space-x-4'>
          {% if settings.social_facebook_link != blank %}
            <a href='{{ settings.social_facebook_link }}' class='text-light hover:text-primary transition-colors'>
              {% render 'icon-facebook' %}
            </a>
          {% endif %}

          {% if settings.social_twitter_link != blank %}
            <a href='{{ settings.social_twitter_link }}' class='text-light hover:text-primary transition-colors'>
              {% render 'icon-twitter' %}
            </a>
          {% endif %}

          {% if settings.social_instagram_link != blank %}
            <a href='{{ settings.social_instagram_link }}' class='text-light hover:text-primary transition-colors'>
              {% render 'icon-instagram' %}
            </a>
          {% endif %}

          {% if settings.social_youtube_link != blank %}
            <a href='{{ settings.social_youtube_link }}' class='text-light hover:text-primary transition-colors'>
              {% render 'icon-youtube' %}
            </a>
          {% endif %}
        </div>
      </div>

      <div>
        <h5 class='text-white font-semibold mb-4'>Shop</h5>
        <ul class='space-y-2'>
          {% for link in linklists['footer-shop'].links %}
            <li>
              <a href='{{ link.url }}' class='text-light hover:text-primary transition-colors'>{{ link.title }}</a>
            </li>
          {% endfor %}
        </ul>
      </div>

      <div>
        <h5 class='text-white font-semibold mb-4'>Learn</h5>
        <ul class='space-y-2'>
          {% for link in linklists['footer-learn'].links %}
            <li>
              <a href='{{ link.url }}' class='text-light hover:text-primary transition-colors'>{{ link.title }}</a>
            </li>
          {% endfor %}
        </ul>
      </div>

      <div>
        <h5 class='text-white font-semibold mb-4'>Support</h5>
        <ul class='space-y-2'>
          {% for link in linklists['footer-support'].links %}
            <li>
              <a href='{{ link.url }}' class='text-light hover:text-primary transition-colors'>{{ link.title }}</a>
            </li>
          {% endfor %}
        </ul>
      </div>
    </div>

    <div class='border-t border-gray-800 pt-8'>
      <div class='flex flex-col md:flex-row justify-between items-center'>
        <p class='text-light/60 text-sm mb-4 md:mb-0'>
          &copy; {{ 'now' | date: '%Y' }}
          {{ shop.name }}. All rights reserved.
        </p>

        <div class='flex space-x-6'>
          {% for link in linklists['footer-utility'].links %}
            <a href='{{ link.url }}' class='text-light/60 hover:text-primary text-sm transition-colors'>
              {{- link.title -}}
            </a>
          {% endfor %}
        </div>
      </div>

      <p class='text-light/40 text-xs mt-8 text-center'>
        *These statements have not been evaluated by the Food and Drug Administration. This product is not intended to
        diagnose, treat, cure, or prevent any disease.
      </p>
    </div>
  </div>
</footer>
